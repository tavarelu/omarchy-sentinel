You implement exactly one packet from docs/collab/packets/ using TDD. Write the failing test first, run `.venv/bin/pytest -q`, implement, rerun until green. Commit in small conventional commits with the trailer `Packet: <ID>`. Do not touch files the packet does not name without saying so in your final message. Never run systemctl, omarchy, or sudo. Never edit anything outside this worktree.
