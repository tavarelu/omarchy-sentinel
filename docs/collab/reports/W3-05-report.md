# W3-05 report
Branch: agents/W3-05   Commits: (see `git log agents/W3-05`, committed after this report)   Tests: 267 passed / 0 failed, 3 skipped (from `.venv/bin/pytest -q`)

## Done
- `src/sentinel/vendors.py` (new): `Vendor`/`ConfigBypass` dataclasses, the `VENDORS` table (claude, codex, grok, gemini, cursor), `bare_bypass_tokens()`/`paired_bypass_flags()` accessors, and the pure `redact_argv(argv) -> tuple[list[str], bool]` (S4).
- `src/sentinel/rules.py`: `extract_bypass_flags` now unions `vendors.VENDORS`' `argv_bypass` (no vendor gating) with `DEFAULT_BYPASS_FLAGS` and the pre-existing `extra_bypass_flags` config-extras param, and detects two-token `--flag value` pairs, reporting them as `--flag=value`. `evaluate_process` now calls `redact_argv` before building the Alert and sets `evidence["redacted"] = True` only when something was actually redacted.
- `src/sentinel/wrap_record.py`: `record_launch` now redacts `cmdline` before the `launches.jsonl` write (the actual on-disk persistence point) and adds a top-level `"redacted"` bool to the record (there is no `evidence` key on a launch record). `launch_to_alert` carries `record["redacted"]` into `alert.evidence["redacted"]` since a second `redact_argv` pass over already-redacted argv finds nothing left to flag.
- `src/sentinel/notify.py`: new `DEFAULT_EXEC_PREFIX = ("omarchy-launch-floating-terminal-with-presentation", "sentinel-action")`, applied to `_exec_tail`, `build_argv`, and `Notifier.__init__`'s `exec_prefix` defaults (all three needed changing — `Notifier.__init__`'s own default is what production and both pinned tests actually exercise, not `build_argv`'s). `_exec_tail` also validates `alert_id` against `^[A-Za-z0-9_.-]+$` and degrades the tail (drops the floating-terminal prefix, keeps the notification) rather than suppressing it, in case something upstream ever hands it an id containing shell metacharacters — see Deviations.
- `src/sentinel/action.py`: `cmd_menu` gained `isatty`/`prompt` keyword params (mirroring `kill.confirm_kill`'s own shape). Non-tty keeps the exact existing cheat-sheet body. Tty shows the alert then the packet's ten-line numbered prompt and dispatches via a new `_dispatch_menu_choice` to the existing `cmd_approve`/`cmd_kill`/`cmd_investigate`/`cmd_summarize`/`cmd_dismiss`. Kill (5/6) still calls `cmd_kill`, which still gates through `kill.confirm_kill` — the menu's own `isatty` is never threaded into that gate, so a menu keypress alone can never substitute for kill's own confirm.
- `scripts/install-wrappers.sh`: after each shim is written, `verify_on_login_path` runs `bash -lc 'command -v <name>'`, canonicalizes both sides (`canon()`, `realpath -m` with a `cd .. && pwd -P` fallback — a raw string compare false-positives on this machine's own PATH, which carries `~/.local/bin` under two spellings) and compares to the shim path; on mismatch or absence it prints both fixes (the `~/.local/share/mise/shims` fix is explicitly rejected in the message) and exits 1 under the script's existing `set -e`.
- `scripts/install-cli.sh` (new): dry-run by default; `--apply` symlinks `sentinel`/`sentinel-action`/`sentinel-scout` from `$SENTINEL_VENV/bin` (default `$XDG_DATA_HOME/<plugin-id>/venv`, matching `install-daemon.sh`'s own convention) into `$SENTINEL_BIN_DIR` (default `~/.local/bin`); no-op when a target already symlinks to the right venv binary; backs up any other existing file/symlink with the same `*.sentinel-bak.<ts>` convention as `install-wrappers.sh`; refuses (rather than silently relinking) a symlink that already resolves into *some* `venv/bin/<name>` unless `SENTINEL_CLI_FORCE=1` is set, so a stray run from the wrong checkout cannot quietly repoint a working live CLI.
- `packaging/omarchy-menu-sentinel.jsonc`: all four actions prefixed with `omarchy-launch-floating-terminal-with-presentation ` (see Deviations for a live-UX caveat on two of the four).
- Tests: `tests/test_vendors.py` (new, 17 tests — vendor table + `extract_bypass_flags` + `redact_argv` + wiring into `evaluate_process`), `tests/test_menu_jsonc.py` (new, 2 tests), `tests/test_install_wrappers.py` (new, 3 tests, subprocess against the real script), `tests/test_install_cli.py` (new, 5 tests, subprocess against the real script), plus extensions to `tests/test_notify.py` (2 pin updates + 3 new), `tests/test_action.py` (7 new interactive-menu tests), `tests/test_wrap_record.py` (3 new redaction-wiring tests).

## Fact-check
| Claim | Verdict | Evidence |
|-------|---------|----------|
| 1 (vendor `--help` flags) | CONFIRMED for claude/codex/grok/gemini; UNVERIFIED for cursor | Ran `grok --help`, `claude --help`, `codex --help` (and `codex exec --help`), `gemini --help` on this machine 2026-09-07. grok: `--always-approve` ("Auto-approve all tool executions"), `--permission-mode [default, acceptEdits, auto, dontAsk, bypassPermissions, plan]`. claude: `--dangerously-skip-permissions`, `--permission-mode <mode>` (choices acceptEdits/auto/bypassPermissions/manual/dontAsk/plan), and a separate `--allow-dangerously-skip-permissions` that only *enables the option* (deliberately not added as a bypass token). codex: `--dangerously-bypass-approvals-and-sandbox`, `-a/--ask-for-approval <on-request\|on-failure\|never\|untrusted>`, `-s/--sandbox <read-only\|workspace-write\|danger-full-access>`. gemini: `-y/--yolo`, `--approval-mode [default\|auto_edit\|yolo\|plan]`. `which cursor-agent cursor` found neither binary — cursor's `VENDORS` entry has empty `argv_bypass` rather than a guessed flag name. |
| 2 (config-file bypass) | CONFIRMED for grok; UNVERIFIED for codex/gemini | `cat ~/.grok/config.toml` (metadata/path read, not the forbidden kind of body-parsing this packet is about) shows `[ui]` … `permission_mode = "always-approve"` verbatim as the packet claims. `~/.codex/config.toml` does not exist on this machine at all (`ls` confirms), so codex's `approval_policy`/`sandbox_mode` key path in `vendors.py` is recorded as a documented placeholder, not a verified value — matches the module's own UNVERIFIED comment. Gemini's settings-file key path is likewise left unset rather than guessed. |
| 3 (PATH order) | REFUTED as literally stated, CONFIRMED as re-measured by the squad; install-truth change still correct | `bash -lc 'echo $PATH; command -v claude'` today: `command -v claude` resolves to `~/.local/share/mise/installs/claude/latest/claude`. `~/.local/bin` sits at PATH position 14 (canonical spelling), behind mise at position 10 — a raw shim there is genuinely shadowed for `claude`. But `~/.local/share/../bin` (the non-canonical uv spelling of the same directory, prepended by `~/.local/bin/env`) sits at position ~10, ahead of mise — so anything already installed under `~/.local/bin` (like the hand-made `sentinel-action` symlink) IS resolvable today. This is exactly the squad's finding: claim 3's blanket "the shim will never run" is wrong, but the install-truth check is still correct and necessary because the canonical spelling a fresh shim gets written to is genuinely shadowed. `install-wrappers.sh`'s new verification step reproduces this correctly (see Acceptance). |
| 4 (`~/.local/bin/claude` etc. are mise launchers, not shims) | CONFIRMED | Not independently re-run this session (read-only inspection of `~/.local/bin/claude`'s content was not necessary to implement this packet); accepted on the strength of five independent analyst passes plus the squad brief's own verification, none of which found this claim false. |
| 5 (omarchy-notification-send / `--exec` / floating terminal) | CONFIRMED | `omarchy-launch-floating-terminal-with-presentation` exists at `/usr/bin` (534 bytes, root-owned, confirmed `file` = bash script) with `/usr/share/omarchy/bin/omarchy-launch-floating-terminal-with-presentation` a symlink to it (matches the squad's correction that claim 5's path was right and an earlier reviewer's "wrong path" note was itself backwards). Read the script directly: `cmd="$*"` then `exec setsid uwsm-app -- xdg-terminal-exec ... -e bash -c "$presentation_script"` where `$presentation_script` embeds `$cmd` — confirms the shell re-parse the squad flagged. This is why `_exec_tail` now validates the alert id before handing it to this wrapper (see Deviations). |
| 6 (CLIs already on PATH from a toast) | REFUTED as "the click path is headless because sentinel-action isn't reachable"; CONFIRMED as re-framed by the squad | `ls -la ~/.local/bin/sentinel*` shows `sentinel`, `sentinel-action`, `sentinel-scout` are symlinks into `~/.local/share/tav.sentinel/venv/bin`, dated Sep 5 00:23 — `sentinel-action` genuinely is resolvable from a toast today. The actual defect this packet fixes on the click-path side is that `cmd_menu` had nothing to say to a user with no tty (a headless invocation just printed a cheat sheet and exited) — `install-cli.sh` formalizes the existing hand-made symlinks per the packet's own Changes note ("formalization of what was done by hand"), which is what I implemented; verified idempotent over a pre-existing correct symlink (`tests/test_install_cli.py::test_idempotent_over_existing_correct_symlink`). |
| 7 (`cmd_menu` prints a cheat sheet and exits, no `isatty` anywhere) | CONFIRMED | Read `src/sentinel/action.py` before editing: `cmd_menu` at (then) line 210 was `get_alert` → print header/summary → `action_lines(alert)` → `return 0`, unconditionally, no `sys.stdin` reference anywhere in the file. |
| 8 (`tests/test_notify.py` pins the old `--exec` tail) | CONFIRMED | Two pins existed: `test_high_is_normal_with_30s_timeout` (full-list equality) and `test_exec_tail_is_last` (`calls[0][-4:]`). Both updated to the new 5-element tail; both green after the edit (see Acceptance). |

## Deviations
- **R-BYPASS-CONFIG (REQ-9 through REQ-11) is deferred, not implemented.** The packet's own "Changes" section assigns it to the scout, but `scout.py` has zero `Alert`/`store.append_alert`/`notify` wiring today, and every one of the five cross-reviewed analyses plus the prior day's five-agent squad review (`docs/collab/reports/SQUAD-BRIEFS-2026-09-06.md`, W3-05 section, "Final position" item C) converged on the same recommendation: split this out and serialize it behind W3-03, because (a) it collides with W3-03 on `vendors.py`'s schema, on `scout.py` itself, and on `watchlist.json`'s v2 schema (W3-03's packet text lists content hashing as its own non-goal deferred to W6-03); (b) a naive implementation risks a real cloud-exposure bug the squad's security analyst found and I independently confirmed by reading `src/sentinel/summarize.py`: `ALLOWED_KEYS` includes `"hashes"` but not `"evidence"`, so a config-file digest stored under `Alert.hashes` (the schema's own dedicated field for exactly this kind of data, per the design spec's Alert-schema example) would ship to `api.x.ai` on a Summarize click, whereas the tests-agent's own proposed assertion already steers toward the safe location (`evidence`) — a natural-looking implementation choice would get this wrong; (c) `docs/collab/LEDGER.md`'s D-005 ("May Sentinel parse agent settings bodies…") is still open, not marked DECIDED, and directly overlaps this rule's own documented limitation; (d) the rule "can only ever say 'this file changed'" per the packet's own design (never the key or value), and the squad's live-evidence citation (the 127-alert R-HOOK-WRITE desensitization storm on 2026-09-05) is a concrete, on-the-record reason to worry about a medium-severity rule whose alert body is this thin. I did not implement `R-BYPASS-CONFIG`, did not add it to `keys.WRITE_RULES` or `investigate.RULE_TEXT`/`_RENDERERS`, and left `watchlist.json`'s schema untouched. This is the packet's largest single scope cut; flagging it explicitly rather than silently dropping it, per "record the deviation" in this session's instructions. The `vendors.py` `ConfigBypass` dataclass and the per-vendor `config_bypass` metadata are still implemented (REQ-1's field), since W3-03 and W6-03 can consume that declarative data later without Sentinel reading any file body in this packet.
- **`_exec_tail` gained an alert-id validation/degrade step beyond what the packet's Changes text asks for.** Reading `/usr/bin/omarchy-launch-floating-terminal-with-presentation` directly (see Fact-check claim 5) confirmed the squad's finding that it re-joins its argv through `bash -c`, making every tail token after `--exec` shell source. `Alert.id` is `str(uuid4())` today so this never fires in practice, but the fix is a five-line addition strictly inside `_exec_tail` (the exact function the packet names) and degrades to the pre-W3-05 headless tail rather than ever dropping a notification — load-bearing because `build_argv` is shared with the sticky R-SELF tamper-alert path, where suppressing the toast would be the worse bug. Covered by two new tests in `test_notify.py`.
- **`packaging/omarchy-menu-sentinel.jsonc` wraps all four rows uniformly, per the packet's explicit Changes text (REQ-22) and the tests it names (REQ-23), even though the squad found a real live-UX defect this creates for two of the four rows**: `omarchy-show-done` (invoked by the floating-terminal wrapper after every command) blocks on `read -rsn 1 </dev/tty` whenever a real tty is present, so `sentinel-action pause 1h` and `sentinel-scout --refresh` — both instant, non-interactive commands today — would each pop open a terminal and then hang waiting for a keypress. I implemented the packet as written rather than unilaterally redesigning which rows get wrapped, since the squad's own suggested fix ("the jsonc test should encode which rows need a tty") is a Chief-level test-design decision, not something to improvise inside an implementation packet. Flagging for the Chief/Owner before this menu file is ever installed for real (it is not — installers are changed, not run, this session).
- **DEFAULT_AGENT_BASENAMES gap (rules.py) noted but not fixed**: `DEFAULT_AGENT_BASENAMES` (used by `evaluate_child_shell`/R-CHILD-SHELL) does not include `"gemini"`, and is duplicated byte-for-byte in `daemon.py`. This is real (confirmed by grep) but is W3-04's territory (R-CHILD-SHELL wiring), not named anywhere in this packet's Changes section; left untouched to keep the diff local per the Chief's scope note.
- **`extract_bypass_flags`'s fingerprint-drift risk is real and unmitigated in this packet, by design.** `keys.alert_flag_set`/`allowlist.fingerprint` key off `evidence.flags` computed at alert-creation time; folding vendor `argv_bypass` into `extract_bypass_flags` (as REQ-2 requires) changes which flags a given cmdline reports, which can silently invalidate previously-approved allowlist entries once this reaches a machine with real approvals on it (the Owner's live machine has ~95, per LEDGER). This is inherent to REQ-2 as specified, not something a scoped implementation of this packet can avoid; flagging it for the Chief/Owner before any live redeploy, exactly as four of the five analyst reports recommended.
- **`docs/superpowers/specs/2026-09-03-omarchy-sentinel-design.md` section 12's example `--exec sentinel-action <alert-id> menu` and bare menu-action snippets are now stale** (they still show the pre-W3-05 headless form). Left untouched — `W3-06 (Docs truth)` is a separate, Chief-owned, in-progress packet per LEDGER, and this packet's scope guardrails say nothing about editing the design spec.
- **`tests/test_install_wrappers.sh` / `tests/test_install_cli.sh` (the exact filenames the packet's Tests section names) were written as `tests/test_install_wrappers.py` / `tests/test_install_cli.py`** — thin Python wrappers driving the real `.sh` scripts via `subprocess.run`, per the prior day's squad brief's explicit recommendation ("propose tests/test_install_wrappers_sh.py::... — pytest does not collect .sh files"). Both new Python files exercise the actual shell scripts end-to-end (not reimplementations), so the packet's intent (drive the installer, assert on exit code and printed text) is preserved.

## Not done
- R-BYPASS-CONFIG (REQ-9, REQ-10, REQ-11) — see Deviations. Needs a Chief/Owner ruling (this session made the call to defer rather than implement something the squad already flagged as "near-useless as specified, and may be worse than nothing" without that ruling).
- `shellcheck scripts/install-wrappers.sh scripts/install-cli.sh` (packet Acceptance line 2) — `shellcheck` is not installed on this machine and installing it needs a package manager (network + likely sudo), which is outside this packet's permitted actions. Reported UNVERIFIED, not faked as passing; `tests/test_install_wrappers.py`/`tests/test_install_cli.py` each carry a `shellcheck`-if-available test that currently reports `skipped` for the same reason, visible in the `.venv/bin/pytest -q` tally (3 skipped: the one pre-existing joint-suite gate plus these two).
- README.md's documented limitation for R-BYPASS-CONFIG (REQ-11's second half) — not needed yet since the rule itself is deferred.
- `keys.WRITE_RULES` / `investigate.RULE_TEXT`/`_RENDERERS` entries for `R-BYPASS-CONFIG` — not needed yet, same reason.

## Asks
- None filed as `docs/collab/asks/ASK-<n>.md` — this session implemented directly rather than through the Grok packet-relay loop, so decisions that would normally become blocking asks are recorded above (Deviations) for the Chief/Owner to rule on directly: (1) confirm or overrule the R-BYPASS-CONFIG deferral before any future packet re-attempts it; (2) decide whether the jsonc click-path wrap should be selective (only rows whose command actually needs a terminal) rather than uniform; (3) note the `extract_bypass_flags` fingerprint-drift risk before any live redeploy touches a machine with real allowlist entries.

## Self-review
- No separate reviewer/auditor subagents were spawned (this session implemented directly as the packet's LEAD, per the task instructions, not through the Grok relay that PROTOCOL.md section 6/AGENTS.md describes for Grok-authored packets). Self-review consisted of: re-reading every edited function against its cited requirement before moving on; running the full suite after each file group (`test_vendors.py`, `test_notify.py`, `test_wrap_record.py`+`test_rules.py`, `test_action.py`, `test_menu_jsonc.py`, the two new install-script test files) rather than only at the end; live-executing both install-wrappers.sh acceptance scenarios and all four install-cli.sh scenarios by hand (shadowed/exit-1, bin-dir-first/exit-0, dry-run/no writes, apply/idempotent/backup) before trusting the pytest wrappers around them; and directly reading `/usr/bin/omarchy-launch-floating-terminal-with-presentation`'s actual source rather than relying on the squad brief's paraphrase, before deciding the `_exec_tail` sanitization was warranted.
- Security-relevant self-check: confirmed no new code path opens a vendor config file's body (`vendors.ConfigBypass` is declarative metadata only, never dereferenced by any function in this packet); confirmed `redact_argv` is wired into both call sites that persist argv (`wrap_record.record_launch` at write time, `rules.evaluate_process` for the in-memory Alert) rather than only unit-tested in isolation; confirmed the new `verify_on_login_path`/`canon` shell functions and `install-cli.sh` never write outside `$SENTINEL_BIN_DIR`/`$BIN_DIR`, never call `systemctl`/`omarchy`/`sudo`, and every test invocation of both scripts sets `SENTINEL_BIN_DIR`/`SENTINEL_VENV` to a `tmp_path` (never the real `~/.local/bin`); confirmed kill dispatch from the interactive menu (`_dispatch_menu_choice` choices 5/6) still calls `cmd_kill` → `confirm_kill` with the menu's own `isatty` kept out of that call, so a menu keypress is never by itself sufficient consent to kill (`test_menu_interactive_dispatch_kill_session_precious_still_requires_tty_phrase` exercises this directly).

## Acceptance (run in this worktree)
```
$ .venv/bin/pytest -q
267 passed, 3 skipped in 3.80s

$ shellcheck scripts/install-wrappers.sh scripts/install-cli.sh
(shellcheck not installed on this machine; UNVERIFIED, not run — see Not done)

$ SENTINEL_BIN_DIR=/tmp/sb PATH=/tmp/fakemise:/tmp/sb:$PATH bash scripts/install-wrappers.sh claude; echo "exit=$?"
installed /tmp/sb/claude → sentinel-wrap claude (real=/tmp/fakemise/claude)
install-wrappers: FATAL: 'claude' is shadowed — the login shell resolves it to /tmp/fakemise/claude, not /tmp/sb/claude
  fix 1: put $BIN_DIR (/tmp/sb) before the mise entries in your shell rc
  fix 2: set SENTINEL_BIN_DIR to a directory that already precedes mise on PATH
         (~/.local/share/mise/shims is NOT acceptable — it IS the shadow)
  re-run: SENTINEL_BIN_DIR=<dir> scripts/install-wrappers.sh claude
exit=1
```

Every previously-passing test still passes; the one pre-existing joint-suite skip (behind the w3-07 gate) is unchanged. Baseline before this packet: 229 passed, 1 skipped. After: 267 passed, 3 skipped (38 net new tests; +2 skips are the honest shellcheck-absent skips described above, not the joint gate).
